[Service]
# Keep the delegated user service at the user-manager score. MemoryLow and
# MemoryMin provide cgroup-level protection without a root score bridge.
OOMScoreAdjust=100
MemoryLow=512M
MemoryMin=256M

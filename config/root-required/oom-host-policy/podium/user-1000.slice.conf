[Slice]
# Bound the whole UID 1000 tree, including user@1000.service and session scopes,
# so app.slice plus an interactive session cannot recreate the host-wide OOM.
MemoryHigh=80G
MemoryMax=96G
MemorySwapMax=8G
MemoryLow=20G
MemoryMin=10G

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hapax-agentgov"
version = "0.2.0"
description = "Computational constitutional governance for AI agent systems"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [{ name = "Ryan Lee", email = "rylklee@gmail.com" }]
keywords = [
    "governance",
    "consent",
    "agents",
    "multi-agent",
    "information-flow",
    "access-control",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Security",
    "Typing :: Typed",
]
dependencies = ["pyyaml>=6.0"]

[project.optional-dependencies]
dev = ["pytest>=9.1.1", "hypothesis>=6.167.1", "ruff>=0.16.5", "pyright>=1.1.411"]

[project.urls]
Homepage = "https://github.com/hapax-systems/agentgov"
Documentation = "https://github.com/hapax-systems/agentgov#readme"
Repository = "https://github.com/hapax-systems/agentgov"
Issues = "https://github.com/hapax-systems/agentgov/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/agentgov"]

[tool.ruff]
line-length = 100
target-version = "py312"
# Markdown is not ours to format. Ruff 0.16 promoted markdown formatting out of preview and README.md
# went red without anyone touching it. Declared here as well as in the root ruff.toml because this
# package's [tool.ruff] makes it the config root for its own subtree — a root-level exclude cannot
# reach it.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "standard"

[tool.pytest.ini_options]
testpaths = ["tests"]

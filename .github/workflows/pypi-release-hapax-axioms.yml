name: "PyPI release: hapax-axioms"

on:
  push:
    tags:
      - "hapax-axioms-v*"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  test:
    name: "test (3.12 + 3.13)"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]
    defaults:
      run:
        working-directory: packages/hapax-axioms
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
      - name: Set Python
        env:
          PYTHON_VERSION: ${{ matrix.python-version }}
        run: uv python install "$PYTHON_VERSION"
      - name: Install package + dev deps
        run: uv sync --extra dev
      - name: Lint
        run: uv run ruff check .
      - name: Format check
        run: uv run ruff format --check .
      - name: Run tests
        run: uv run pytest -q

  build:
    name: build sdist + wheel
    runs-on: ubuntu-latest
    needs: test
    timeout-minutes: 5
    defaults:
      run:
        working-directory: packages/hapax-axioms
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
      - name: Build distributions
        run: uv build --sdist --wheel
      - name: Read package version
        id: version
        run: |
          set -eo pipefail
          v=$(grep -m1 -E '^version\s*=' pyproject.toml | sed -E 's/^version *= *"([^"]+)".*/\1/')
          echo "version=$v" >> "$GITHUB_OUTPUT"
      - name: Upload artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: hapax-axioms-dist
          path: packages/hapax-axioms/dist/
          if-no-files-found: error

  publish:
    name: publish to PyPI
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push'
    timeout-minutes: 10
    environment:
      name: pypi
      url: https://pypi.org/project/hapax-axioms/
    permissions:
      id-token: write  # OIDC trusted publishing
    steps:
      - name: Download distributions
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: hapax-axioms-dist
          path: dist/
      - name: Publish (trusted publisher)
        uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33
        with:
          packages-dir: dist/
          skip-existing: true
          print-hash: true

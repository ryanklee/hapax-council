name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    types: [checks_requested]

permissions:
  actions: read
  contents: read
  pull-requests: read
  security-events: write

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  post_merge_duplicate_filter:
    runs-on: ubuntu-latest
    outputs:
      duplicate_merge_group: ${{ steps.detect.outputs.duplicate_merge_group }}
    steps:
      - name: Detect duplicate post-merge push
        id: detect
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          duplicate_merge_group=false
          if [ "$GITHUB_EVENT_NAME" = "push" ]; then
            if run_count="$(
              gh api \
                "/repos/$GITHUB_REPOSITORY/actions/workflows/ci.yml/runs?head_sha=$GITHUB_SHA&event=merge_group&status=success&per_page=1" \
                --jq '.total_count'
            )"; then
              if [ "${run_count:-0}" -gt 0 ]; then
                duplicate_merge_group=true
              fi
            else
              echo "Could not query prior merge-group CI runs; running full CI."
            fi
          fi

          echo "duplicate_merge_group=$duplicate_merge_group" >> "$GITHUB_OUTPUT"
          if [ "$duplicate_merge_group" = true ]; then
            echo "Merge queue already validated this SHA; push run will report sentinels."
          else
            echo "No prior successful merge-group CI found for this SHA."
          fi

  docs_only_filter:
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.diff.outputs.docs_only }}
      changed_count: ${{ steps.diff.outputs.changed_count }}
      python_prod_dependency_witness: ${{ steps.diff.outputs.python_prod_dependency_witness }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - name: Detect docs-only change set
        id: diff
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail

          changed_files="$(mktemp)"
          merge_group_base_sha=""
          if [ "$GITHUB_EVENT_NAME" = "merge_group" ]; then
            merge_group_base_sha="$(
              printf '%s\n' "$GITHUB_REF_NAME" \
                | sed -n 's/.*-\([0-9a-f]\{40\}\)$/\1/p'
            )"
          fi

          if [ -n "$PR_BASE_SHA" ]; then
            git fetch --quiet origin "$PR_BASE_SHA" || true
            git diff --no-renames --name-only "$PR_BASE_SHA"...HEAD > "$changed_files"
          elif [ -n "$merge_group_base_sha" ]; then
            git fetch --quiet origin "$merge_group_base_sha" || true
            git diff --no-renames --name-only "$merge_group_base_sha"..HEAD > "$changed_files"
          else
            if [ -z "$PUSH_BEFORE_SHA" ] || [ "$PUSH_BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
              echo "docs_only=false" >> "$GITHUB_OUTPUT"
              echo "changed_count=0" >> "$GITHUB_OUTPUT"
              echo "No stable base SHA for $GITHUB_EVENT_NAME; running full CI."
              exit 0
            fi
            git fetch --quiet origin "$PUSH_BEFORE_SHA" || true
            git diff --no-renames --name-only "$PUSH_BEFORE_SHA" HEAD > "$changed_files"
          fi

          sed -i '/^$/d' "$changed_files"
          changed_count="$(wc -l < "$changed_files" | tr -d ' ')"

          docs_only=true
          python_prod_dependency_witness=false
          if [ "$changed_count" -eq 0 ]; then
            docs_only=false
          fi

          is_audio_authority_path() {
            local path="$1"
            case "$path" in
              docs/audio-topology-reference.md|docs/audio|docs/audio/*) return 0 ;;
              config/audio-*|config/pipewire/*|config/wireplumber/*) return 0 ;;
              config/hapax/audio-*) return 0 ;;
            esac
            return 1
          }

          is_system_dynamics_map_authority_path() {
            local path="$1"
            case "$path" in
              docs/architecture/system-dynamics-map*|docs/architecture/vendor/cytoscape-*.js) return 0 ;;
            esac
            return 1
          }

          docs_only_path() {
            local path="$1"
            if is_audio_authority_path "$path" || is_system_dynamics_map_authority_path "$path"; then
              return 1
            fi
            case "$path" in
              docs|docs/*|lab-journal|lab-journal/*|research|research/*)
                return 0
                ;;
            esac
            if [[ "$path" == *.md && "$path" != */* ]]; then
              return 0
            fi
            if [[ "$path" == axioms/*.md ]]; then
              return 0
            fi
            return 1
          }

          while IFS= read -r path; do
            [ -z "$path" ] && continue
            if ! docs_only_path "$path"; then
              docs_only=false
            fi
            case "$path" in
              .github/workflows/ci.yml|pyproject.toml|uv.lock|tests/test_ci_required_coverage_claims.py|tests/test_python_prod_dependency_constraints.py)
                python_prod_dependency_witness=true
                ;;
            esac
            if [ "$docs_only" = false ] && [ "$python_prod_dependency_witness" = true ]; then
              break
            fi
          done < "$changed_files"

          {
            echo "docs_only=$docs_only"
            echo "changed_count=$changed_count"
            echo "python_prod_dependency_witness=$python_prod_dependency_witness"
          } >> "$GITHUB_OUTPUT"
          if [ "$docs_only" = true ]; then
            echo "Docs-only change set detected; required jobs will report sentinel success."
          else
            echo "Non-docs changes detected; required jobs will run full checks."
          fi

  public_surface_freshness:
    needs: [post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        run: echo "Merge queue already validated this SHA; public-surface freshness reports success."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Public surface registry contract verify
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: uv run --no-project --with pydantic==2.13.4 --with pyyaml==6.0.3 python scripts/check-public-surface-claims.py --check-registry-only
      - name: Value-prop registry contract verify
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: uv run --no-project --with pyyaml==6.0.3 python scripts/check-value-prop-registry.py

  lint:
    needs: [docs_only_filter, post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        run: echo "Merge queue already validated this SHA; lint reports success."
      - name: Docs-only required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only == 'true'
        run: echo "Docs-only change set; lint reports success without running heavy gates."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      # The action is SHA-pinned but installs whatever ruff is newest unless `version:` is given, so
      # a ruff release can change this gate's behaviour with no commit here. That is exactly what
      # happened: ruff 0.16 promoted MARKDOWN formatting out of preview, ~24 untouched docs went red,
      # `lint` failed, `all-green` failed with it, and because all-green is REQUIRED the whole merge
      # queue jammed — hapax-council merged nothing between 2026-07-11 and 2026-08-03 while 75 PRs
      # piled up. Pin the tool, not just the action.
      - uses: astral-sh/ruff-action@278981a28ce3188b1e39527901f38254bf3aac89
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        with:
          version: "0.16.1"
          args: "check"
      # The markdown exclusion is DECLARED IN CONFIG, not passed here. An `--exclude *.md` in this
      # args string is unquoted when the action interpolates it into a shell, so it glob-expands
      # against the repo root (AGENTS.md, CLAUDE.md, CONTRIBUTING.md, GOVERNANCE.md, NOTICE.md,
      # README.md) and silently becomes "--exclude <those six>" plus stray positional args — the
      # nested docs it was meant to cover would still be checked. Config cannot be word-split.
      - uses: astral-sh/ruff-action@278981a28ce3188b1e39527901f38254bf3aac89
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        with:
          version: "0.16.1"
          args: "format --check"
      # Task #155 Stage 5 — anti-personification fail-loud CI gate.
      # Scans axioms/**/*.md and prompt-surface modules; exits non-zero
      # on any deny-list finding that survives the carve-out windows.
      - name: Install system deps (cairo, gobject)
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y libcairo2-dev libgirepository-2.0-dev gobject-introspection
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv sync --extra ci
      - name: Unused-function gate
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          if [ -n "$PR_BASE_SHA" ]; then
            git fetch --quiet origin "$PR_BASE_SHA" || true
            uv run python scripts/check-unused-functions.py --diff-range "$PR_BASE_SHA..HEAD"
          else
            if [ -z "$PUSH_BEFORE_SHA" ] || [ "$PUSH_BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
              echo "no base SHA; skipping unused-function diff gate"
              exit 0
            fi
            git fetch --quiet origin "$PUSH_BEFORE_SHA" || true
            uv run python scripts/check-unused-functions.py --diff-range "$PUSH_BEFORE_SHA..HEAD"
          fi
      - name: new-module-consumer-check
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          if [ -n "$PR_BASE_REF" ]; then
            git fetch --quiet origin "$PR_BASE_REF:refs/remotes/origin/$PR_BASE_REF" || true
            uv run python scripts/check-new-module-consumers.py --base-ref "origin/$PR_BASE_REF"
          elif [ -n "$PR_BASE_SHA" ]; then
            git fetch --quiet origin "$PR_BASE_SHA" || true
            uv run python scripts/check-new-module-consumers.py --diff-range "$PR_BASE_SHA...HEAD"
          else
            if [ -z "$PUSH_BEFORE_SHA" ] || [ "$PUSH_BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
              echo "no base SHA; skipping new-module-consumer-check"
              exit 0
            fi
            git fetch --quiet origin "$PUSH_BEFORE_SHA" || true
            uv run python scripts/check-new-module-consumers.py --diff-range "$PUSH_BEFORE_SHA..HEAD"
          fi
      # taxonomy-A1 — consumer-existence gate. Refuses PRs that add a
      # producer (agent entry module, publication surface, Qdrant
      # collection writer) with zero verified non-test consumers.
      # Effect-based (AST / structured-directive parse), fail-closed on
      # dynamic collection names; governed exit via
      # scripts/producer-consumer-allowlist.json (reason mandatory).
      # Canaries: tests/scripts/test_check_producer_consumers.py.
      - name: consumer-existence-gate
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          if [ -n "$PR_BASE_REF" ]; then
            git fetch --quiet origin "$PR_BASE_REF:refs/remotes/origin/$PR_BASE_REF" || true
            uv run python scripts/check-producer-consumers.py --base-ref "origin/$PR_BASE_REF"
          elif [ -n "$PR_BASE_SHA" ]; then
            git fetch --quiet origin "$PR_BASE_SHA" || true
            uv run python scripts/check-producer-consumers.py --diff-range "$PR_BASE_SHA...HEAD"
          else
            if [ -z "$PUSH_BEFORE_SHA" ] || [ "$PUSH_BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
              echo "no base SHA; skipping consumer-existence-gate"
              exit 0
            fi
            git fetch --quiet origin "$PUSH_BEFORE_SHA" || true
            uv run python scripts/check-producer-consumers.py --diff-range "$PUSH_BEFORE_SHA..HEAD"
          fi
      # system-dynamics map freshness gate. The architecture map
      # (docs/architecture/system-dynamics-map.* + schemas/system-dynamics-map/)
      # is codebase-derived, so any structural change can stale it. Its currency
      # test (test_materialized_semantic_artifacts_are_current) lives in the
      # merge_group full suite (test-full-shard), which is SKIPPED on
      # pull_request — so without this gate a structural PR passes PR CI and only
      # fails once queued, ambushing the merge queue. The materializer is
      # deterministic and stdlib-only; running it here and diffing shifts the
      # check left to PR time. Source: scripts/system_dynamics_map_materialize.py,
      # tests/test_system_dynamics_map_artifacts.py.
      - name: System-dynamics map freshness gate
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: |
          uv run python scripts/system_dynamics_map_materialize.py
          if ! git diff --quiet -- 'docs/architecture/system-dynamics-map*' 'schemas/system-dynamics-map'; then
            echo "::error::system-dynamics architecture map is stale. It is codebase-derived and your changes altered it. Fix: run 'uv run python scripts/system_dynamics_map_materialize.py', then 'git add -- docs/architecture/system-dynamics-map.* schemas/system-dynamics-map/' and commit." >&2
            git --no-pager diff --stat -- 'docs/architecture/system-dynamics-map*' 'schemas/system-dynamics-map' >&2
            exit 1
          fi
          echo "system-dynamics map is fresh."
      - name: Anti-personification linter
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/lint_personification.py
      # audit-F — audio yaml ↔ conf consistency. Fails when a yaml-
      # declared LADSPA chain has no corresponding hapax-*.conf on
      # disk (or vice-versa, unless allowlisted in
      # config/audio-conf-allowlist.yaml). Catches the "configured a
      # chain but forgot the conf" drift class that silently breaks
      # the live audio graph.
      - name: Audio yaml conf consistency verify
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/check-audio-conf-consistency.py
      # ytb-AUTH1 — aesthetic library integrity. Verifies _manifest.yaml
      # and _NOTICES.md match on-disk bytes and every asset's SHA-256
      # still agrees with its manifest entry. Fail-loud if a regenerated
      # asset bypassed the manifest refresh.
      - name: Aesthetic library verify
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/verify-aesthetic-library.py
      # AUDIT-22 Phase B-2 — publication-contract redaction linter.
      # Validates every contract `redactions:` entry is either a
      # registered transform (REDACTION_TRANSFORMS) or a dict-key
      # pattern (contains `.` or ends with `*`). Catches typos /
      # unregistered transforms that would silently no-op in the
      # _apply_redactions wire-in.
      - name: Publication redaction transforms verify
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/verify-redaction-transforms.py
      # V5 weave wk1 d4 — polysemic-audit CI gate (epsilon-owned).
      # Walks docs/audience/ + docs/published-artifacts/ for any *.md
      # artifact and fails when the audit flags cross-domain readings
      # (esp. legal/governance/AI-safety polysemy). Per V5 weave § 12
      # invariant 5: every artifact passes this gate before approval-
      # queue entry.
      - name: Polysemic audit verify
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/verify-polysemic-audit.py
      # Bayesian Phase 0 FULL — HPX003 + HPX004 claim/LR registry gates.
      # HPX003: every signal in shared/lr_registry.yaml validates against
      # LRDerivation. HPX004: every claim has a PriorProvenance entry in
      # shared/prior_provenance.yaml. Operator directive 2026-04-24T22:40Z
      # (verbatim): "Lack of priors is itself a prior; priors must be
      # derivations of invariants, not ad-hoc."
      - name: HPX003+HPX004 claim/LR registry validators
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/check-claim-registry.py

      # Preset-family affordance closure (categories A + B from
      # scripts/audit-preset-affordances.py). Catches thin families
      # (<3 members) and FAMILY_PRESETS entries missing on disk —
      # both are critical regressions of the preset-variety guarantee.
      # Qdrant drift (category D) is skipped via --no-qdrant since CI
      # has no Qdrant access. The pytest regression guard at
      # tests/studio_compositor/test_preset_family_selector.py
      # (TestEveryFamilyHasCapability, PR #2832) covers the
      # family-capability invariant from the other direction.
      - name: Preset-family affordance closure
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/audit-preset-affordances.py --no-qdrant

      # Operator legal-name leak guard. Per `interpersonal_transparency`
      # axiom + the 2026-04-24 operator-referent policy, the operator's
      # legal name appears only in formal-context locations
      # (axioms/contracts/, profiles/, .github/CODEOWNERS). Any other
      # occurrence in source/docs/config is a leak. The script's
      # whitelist is the authoritative allowed-locations list.
      # SHAs are not user-input but env: pattern is used per safe-CI
      # idiom regardless.
      - name: Operator legal-name leak guard
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          if [ -n "$PR_BASE_SHA" ]; then
            git fetch --quiet origin "$PR_BASE_SHA" || true
            bash scripts/check-legal-name-leaks.sh --diff "$PR_BASE_SHA..$PR_HEAD_SHA"
          else
            if [ -z "$PUSH_BEFORE_SHA" ] || [ "$PUSH_BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
              echo "no base SHA; skipping (initial push or shallow checkout)"
              exit 0
            fi
            git fetch --quiet origin "$PUSH_BEFORE_SHA" || true
            bash scripts/check-legal-name-leaks.sh --diff "$PUSH_BEFORE_SHA..HEAD"
          fi
      # Cross-runtime gate-manifest wiring check (reform §6 P0 — one gate-set
      # SSOT). Verifies the codex hook adapter, antigrav/vibe capability markers,
      # and CI jobs/run-markers still match hooks/gate-manifest.yaml.
      # The live ~/.claude/settings.json check has no file on CI runners, so it
      # is skipped here and enforced by the pre-commit hook on the operator
      # machine where the live settings exist.
      - name: Gate manifest wiring check
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run python scripts/gate-manifest-check.py --skip-claude-settings

  capability-surface-delta:
    needs: [docs_only_filter, post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        run: echo "Merge queue already validated this SHA; capability-surface-delta reports success."
      - name: Docs-only required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only == 'true'
        run: echo "Docs-only change set; capability-surface-delta reports success without running the inventory gate."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Run capability surface delta gate
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run --no-project --with pydantic==2.13.4 --with pyyaml==6.0.3 python scripts/hapax-capability-surface-delta-gate


  egress-boundary-pin:
    # The audio/live-egress release class's BEHAVIORAL arm-time evidence
    # (LIVE_EGRESS_MITIGATION_CHECKS, shared/release_gate.py): runs the
    # egress-behavior pins on every PR and in the merge queue. At main today
    # those pins are authority-before-send, fail-closed refusal, and
    # type-level send capability; the wired-send and receipt-privacy pins
    # arrive with #4440's diff and this job executes them at that head from
    # then on — so no wired send can land without its privacy pins green.
    # The PR admission slice deliberately does NOT include this file; without
    # this job the class's behavioral evidence existed only at merge-queue
    # time (test-full-shard). The pin set is structurally pinned with semantic
    # anchors by tests/ci/test_release_gate_ci_composition.py; semantic fidelity
    # is the quorum's layer, as for every test file in the repo.
    # Unlike sibling jobs this job has NO docs-only sentinel: the class's
    # behavioral evidence must always execute (~40s); a green check that ran
    # nothing is not evidence. The duplicate-merge-group sentinel stays — it
    # means the queue already validated the SHA, never that the pins were
    # skipped. Required transitively: branch protection requires all-green,
    # and all-green needs this job (single-aggregate required-context design).
    needs: [post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        run: echo "Merge queue already validated this SHA; egress-boundary-pin reports success."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Run egress-boundary behavior pins
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
        run: >
          uv run --no-project --with pytest==9.0.2 --with pyyaml --with pydantic
          pytest --confcutdir=tests tests/test_capability_adapter_protocol.py -q --tb=short


  typecheck:
    needs: [docs_only_filter, post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        run: echo "Merge queue already validated this SHA; typecheck reports success."
      - name: Docs-only required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only == 'true'
        run: echo "Docs-only change set; typecheck reports success without running pyrefly."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      # Keep the typecheck path independent of the full CI environment.
      # Pyrefly does not need the cairo/gobject runtime deps or uv sync, and
      # the previous analysis-cache restore did not materially shorten this
      # check.
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        run: uv run --no-project --with pyrefly==0.64.1 pyrefly check

  test-full-shard:
    needs: [docs_only_filter, post_merge_duplicate_filter]
    if: github.event_name == 'merge_group' && needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
    name: test-full-shard (${{ matrix.shard }}/${{ matrix.shard_count }})
    runs-on: ubuntu-latest
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
        shard_count: [4]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        with:
          enable-cache: true
          restore-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
          save-cache: false
      - name: Install system deps (cairo, gobject, gst, pango, hook transports)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcairo2-dev libgirepository-2.0-dev gobject-introspection \
            gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0 \
            gir1.2-pango-1.0 \
            ffmpeg \
            portaudio19-dev \
            socat
      - name: Install Px437 IBM VGA 8x16 font (HARDM aesthetic dependency, audit 4.7 PR #1109)
        run: |
          # Install system-wide so fontconfig's /usr/share/fonts scan picks it up.
          sudo mkdir -p /usr/share/fonts/TTF
          sudo cp assets/aesthetic-library/fonts/Px437_IBM_VGA_8x16.ttf /usr/share/fonts/TTF/
          # Root cache (scans /usr/share/fonts, writes /var/cache/fontconfig).
          sudo fc-cache -fv /usr/share/fonts
          # User cache. Pango enumerates through the client's fontconfig
          # context; without a user cache, Pango can silently omit freshly
          # installed system fonts (PR #1109 run-24641458881 observed).
          fc-cache -fv
          # fc-list probe confirms fontconfig sees the font.
          fc-list | grep -i 'Px437.*IBM VGA 8x16' \
            || (echo "font install failed (fc-list)" >&2; exit 1)
      - name: Sync uv ci extras for full pytest
        run: uv sync --extra ci --frozen
      - name: Run python-prod optional dependency witness
        if: matrix.shard == 1 && needs.docs_only_filter.outputs.python_prod_dependency_witness == 'true'
        # Null-route networked clients so import smokes cannot reach live services from CI.
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          uv run --frozen \
            --extra ci --extra audio --extra tui --extra studio --extra rerank \
            pytest tests/test_python_prod_dependency_constraints.py -q -rs
      - name: Install Playwright Chromium
        run: uv run --no-sync playwright install chromium
      - name: Verify Pango can resolve Px437 IBM VGA 8x16
        # Pango consults fontconfig but has its own per-process cache; a
        # Pango-level probe confirms the HARDM render path will see the
        # font, not just fontconfig. If this fails, HARDM tests render
        # blank cells silently, so fail loud here with diagnostic output.
        run: |
          uv run --no-sync python scripts/ci_verify_pango_font.py "Px437 IBM VGA 8x16"
      - name: Run full pytest shard
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
          PYTEST_SHARD: ${{ matrix.shard }}
          PYTEST_SHARD_COUNT: ${{ matrix.shard_count }}
        run: |
          # pytest hangs post-session due to open sockets from module-level
          # LiteLLM/Qdrant clients. Root conftest.py has a pytest_sessionfinish
          # that force-closes cached Qdrant + LiteLLM + httpx clients, so
          # teardown exits cleanly (~0.1s) instead of hanging to the timeout.
          #
          # Parallelism: pytest-xdist -n 4 --dist loadfile runs four worker
          # processes, distributed per-file so tests within a file stay
          # serial (preserves file-scoped fixture state + /dev/shm and
          # /tmp write patterns). GitHub Actions ubuntu-latest has 4 vCPUs;
          # -n 4 fully saturates the runner. Bumped from -n 2 in CI/CD
          # speedup PR1 (research drop docs/research/2026-04-26-cicd-speedup.md
          # §2 #2). Monitor first 3 PRs for OOM or socket-conflict regressions;
          # if a flake surfaces, fall back to -n 3.
          #
          # Timeout reduced 25m → 20m after socket-hang fix + xdist. Recent
          # compositor/runtime PRs exercise enough slow end-band tests that
          # the prior 15m wrapper killed otherwise-successful runs at 99%.
          #
          # Merge-queue throughput: full pytest remains mandatory before
          # release, but the queue runs it in four runtime-weighted shards and
          # can split explicitly configured slow files by pytest node prefix.
          # The required `test` check remains the aggregate gate.
          # The shard list is derived from the same collect/ignore/deselect
          # expression used by the actual test command, preserving the
          # existing release surface while reducing longest-runner wall time.
          collect_output="$(mktemp)"
          shard_files="$(mktemp)"
          duration_artifact="/tmp/pytest-node-durations-shard-${PYTEST_SHARD}-of-${PYTEST_SHARD_COUNT}.yaml"
          selected_units_artifact="/tmp/pytest-selected-units-shard-${PYTEST_SHARD}-of-${PYTEST_SHARD_COUNT}.txt"
          uv run --no-sync pytest tests/ --collect-only -q \
            --benchmark-disable \
            --ignore=tests/hapax_daimonion --ignore=tests/contract \
            --ignore=tests/test_frame_gate.py \
            --ignore=tests/test_hapax_daimonion_pipecat_tts.py \
            --ignore=tests/test_hapax_daimonion_pipeline.py \
            --ignore=tests/test_perception_integration.py \
            --ignore=tests/test_sensor_tier2_tier3.py \
            --ignore=tests/fortress \
            --ignore=tests/test_demo_screenshots.py \
            --ignore=tests/test_demo_screencasts.py \
            --ignore=tests/test_demo_title_cards.py \
            --ignore=tests/test_demo_video_integration.py \
            --ignore=tests/test_demo_video.py \
            --ignore=tests/test_demo_timeline.py \
            --ignore=tests/test_event_log_trace_id.py \
            --ignore=tests/studio_compositor/test_visual_regression_homage.py \
            --ignore=tests/studio_compositor/test_follow_mode.py \
            --ignore=tests/studio_compositor/test_face_obscure_pipeline.py \
            --ignore=tests/test_camera_pipeline_phase2.py \
            --ignore=tests/studio_compositor/test_captions_in_default_layout.py \
            --deselect=tests/studio_compositor/test_assignment_nondestructive.py::test_default_layout_informational_wards_are_non_destructive \
            --deselect=tests/studio_compositor/test_compositor_wiring.py::TestStartLayoutOnly::test_reads_disk_layout_and_populates_state_and_registry \
            --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_exists_and_is_valid_layout \
            --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_source_backends_match_registry_dispatch \
            --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_chat_ambient_binds_to_chat_ambient_ward \
            --deselect=tests/studio_compositor/test_default_layout_loading.py::test_load_layout_or_fallback_reads_valid_file \
            --deselect=tests/studio_compositor/test_default_layout_loading.py::test_fallback_layout_parses_to_same_shape_as_default_json \
            --deselect=tests/studio_compositor/test_default_layout_loading.py::test_load_layout_or_fallback_rescales_to_canvas_size \
            --deselect=tests/studio_compositor/test_overlay_zones_research_gate.py::TestZoneGate::test_default_always_on \
            -m "not llm" -k "not golden and not goldens" \
            -p no:cacheprovider > "$collect_output"
          uv run --no-sync python scripts/ci_select_pytest_shard.py \
            --collect-output "$collect_output" \
            --weights config/ci/python-test-runtime-weights.yaml \
            --shard "$PYTEST_SHARD" \
            --shards "$PYTEST_SHARD_COUNT" > "$shard_files"
          unit_count="$(wc -l < "$shard_files" | tr -d ' ')"
          echo "Shard $PYTEST_SHARD/$PYTEST_SHARD_COUNT selected $unit_count test units."
          cp "$shard_files" "$selected_units_artifact"
          if [ "$unit_count" -eq 0 ]; then
            uv run --no-sync python scripts/ci_select_pytest_shard.py \
              --pytest-output /dev/null \
              --duration-artifact "$duration_artifact" \
              --selected-units "$selected_units_artifact" \
              --shard "$PYTEST_SHARD" \
              --shards "$PYTEST_SHARD_COUNT" \
              --run-id "$GITHUB_RUN_ID" \
              --run-attempt "$GITHUB_RUN_ATTEMPT" \
              --head-sha "$GITHUB_SHA" \
              --event-name "$GITHUB_EVENT_NAME"
            echo "No test units selected for this shard."
            exit 0
          fi
          set +e
          timeout -s KILL 1200 \
            xargs -d '\n' -a "$shard_files" uv run --no-sync pytest -q --tb=line \
              --durations=0 --durations-min=0 \
              --benchmark-disable \
              -n 4 --dist loadfile \
              --ignore=tests/hapax_daimonion --ignore=tests/contract \
              --ignore=tests/test_frame_gate.py \
              --ignore=tests/test_hapax_daimonion_pipecat_tts.py \
              --ignore=tests/test_hapax_daimonion_pipeline.py \
              --ignore=tests/test_perception_integration.py \
              --ignore=tests/test_sensor_tier2_tier3.py \
              --ignore=tests/fortress \
              --ignore=tests/test_demo_screenshots.py \
              --ignore=tests/test_demo_screencasts.py \
              --ignore=tests/test_demo_title_cards.py \
              --ignore=tests/test_demo_video_integration.py \
              --ignore=tests/test_demo_video.py \
              --ignore=tests/test_demo_timeline.py \
              --ignore=tests/test_event_log_trace_id.py \
              --ignore=tests/studio_compositor/test_visual_regression_homage.py \
              --ignore=tests/studio_compositor/test_follow_mode.py \
              --ignore=tests/studio_compositor/test_face_obscure_pipeline.py \
              --ignore=tests/test_camera_pipeline_phase2.py \
              --ignore=tests/studio_compositor/test_captions_in_default_layout.py \
              --deselect=tests/studio_compositor/test_assignment_nondestructive.py::test_default_layout_informational_wards_are_non_destructive \
              --deselect=tests/studio_compositor/test_compositor_wiring.py::TestStartLayoutOnly::test_reads_disk_layout_and_populates_state_and_registry \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_exists_and_is_valid_layout \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_source_backends_match_registry_dispatch \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_chat_ambient_binds_to_chat_ambient_ward \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_load_layout_or_fallback_reads_valid_file \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_fallback_layout_parses_to_same_shape_as_default_json \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_load_layout_or_fallback_rescales_to_canvas_size \
              --deselect=tests/studio_compositor/test_overlay_zones_research_gate.py::TestZoneGate::test_default_always_on \
              -m "not llm" -k "not golden and not goldens" \
              -p no:cacheprovider 2>&1 | tee /tmp/pytest-output.txt
          PYTEST_EXIT=${PIPESTATUS[0]}
          uv run --no-sync python scripts/ci_select_pytest_shard.py \
            --pytest-output /tmp/pytest-output.txt \
            --duration-artifact "$duration_artifact" \
            --selected-units "$selected_units_artifact" \
            --shard "$PYTEST_SHARD" \
            --shards "$PYTEST_SHARD_COUNT" \
            --run-id "$GITHUB_RUN_ID" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --head-sha "$GITHUB_SHA" \
            --event-name "$GITHUB_EVENT_NAME" \
            --require-durations
          DURATION_EXIT=$?
          if [ "$DURATION_EXIT" -ne 0 ]; then
            echo "Could not write pytest node duration artifact (exit $DURATION_EXIT)"
            exit 1
          fi
          # pytest exit codes: 0 = all passed, 1 = failures, 2 = usage error,
          # 3 = internal error, 4 = usage error, 5 = no tests collected.
          # With conftest.py pytest_sessionfinish (#1318) closing socket hangs,
          # the timeout wrapper no longer fires on success, so pytest's own exit
          # code is reliable. The prior grep-for-"failed" parse had a bug:
          # "xfailed" contains "failed" as a substring, so any PR with xfail
          # markers (e.g. #1322 chronic-xfails) flagged a false failure.
          # Trust the exit code directly.
          if [ "$PYTEST_EXIT" -eq 0 ]; then
            # Sanity: a success must have a pytest summary line.
            if grep -qE '\bpassed\b' /tmp/pytest-output.txt; then
              echo "All tests passed (pytest exit 0)"
              exit 0
            fi
            echo "No test summary found — likely crashed or timed out"
            exit 1
          fi
          if [ "$PYTEST_EXIT" -eq 124 ] || [ "$PYTEST_EXIT" -eq 137 ]; then
            # SIGTERM (124) or SIGKILL (137) from the ``timeout`` wrapper.
            echo "Test step timed out (exit $PYTEST_EXIT)"
            exit 1
          fi
          echo "Tests failed (pytest exit $PYTEST_EXIT)"
          exit 1
      - name: Upload pytest node duration artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: pytest-node-durations-shard-${{ matrix.shard }}-of-${{ matrix.shard_count }}
          path: |
            /tmp/pytest-node-durations-shard-${{ matrix.shard }}-of-${{ matrix.shard_count }}.yaml
            /tmp/pytest-selected-units-shard-${{ matrix.shard }}-of-${{ matrix.shard_count }}.txt
          if-no-files-found: warn
          retention-days: 30
      - name: Run serial title-card tests in shard 4
        if: matrix.shard == 4
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          timeout -s KILL 600 \
            uv run --no-sync pytest \
              tests/test_demo_title_cards.py \
              tests/test_demo_video_integration.py \
              -q --tb=line --benchmark-disable

  test-title-cards:
    needs: [docs_only_filter, post_merge_duplicate_filter, test-full-shard]
    if: always() && github.event_name == 'merge_group' && needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Report serial title-card shard result
        env:
          TEST_FULL_SHARD_RESULT: ${{ needs.test-full-shard.result }}
        run: |
          set -euo pipefail
          if [ "$TEST_FULL_SHARD_RESULT" = "success" ]; then
            echo "Serial title-card tests ran inside test-full-shard shard 4."
            exit 0
          fi
          echo "Full pytest shard matrix result: $TEST_FULL_SHARD_RESULT"
          echo "Serial title-card coverage is included in shard 4, so this check fails with the shard matrix."
          exit 1

  test:
    needs: [docs_only_filter, post_merge_duplicate_filter, test-full-shard, test-title-cards]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        run: echo "Merge queue already validated this SHA; test reports success."
      - name: Docs-only required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only == 'true'
        run: echo "Docs-only change set; test reports success without running pytest."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && github.event_name != 'merge_group'
      - name: Determine Python test mode
        id: test_mode
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            echo "mode=pr-admission" >> "$GITHUB_OUTPUT"
            echo "Pull request: running fast Python admission slice."
            echo "Merge queue keeps full pytest in required-check shards."
          elif [ "$GITHUB_EVENT_NAME" = "merge_group" ]; then
            echo "mode=merge-group-shards" >> "$GITHUB_OUTPUT"
            echo "Merge queue: aggregating full pytest shard results."
          else
            echo "mode=full" >> "$GITHUB_OUTPUT"
            echo "Non-PR event: running full pytest gate."
          fi
      - name: Verify merge-queue full pytest shards
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'merge-group-shards'
        env:
          TEST_FULL_SHARD_RESULT: ${{ needs.test-full-shard.result }}
          TEST_TITLE_CARDS_RESULT: ${{ needs.test-title-cards.result }}
        run: |
          set -euo pipefail
          if [ "$TEST_FULL_SHARD_RESULT" = "success" ] && [ "$TEST_TITLE_CARDS_RESULT" = "success" ]; then
            echo "All full pytest shards and serial title-card tests passed; required test check reports success."
            exit 0
          fi
          echo "Full pytest shard result: $TEST_FULL_SHARD_RESULT"
          echo "Serial title-card result: $TEST_TITLE_CARDS_RESULT"
          exit 1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode != 'merge-group-shards'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Run PR Python admission slice
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'pr-admission'
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
          PYTHONPATH: ${{ github.workspace }}
        run: |
          uv run --no-project --with pytest==9.0.2 --with pyyaml pytest \
            --confcutdir=tests \
            tests/ci/test_self_hosted_runner_experiment.py \
            tests/ci/test_python_test_throughput_policy.py \
            tests/test_ci_required_coverage_claims.py \
            tests/shared/test_ci_discovery.py \
            tests/scripts/test_screwm_aggregate_port_owner_gate.py \
            tests/systemd/test_source_activation_rooted_python_units.py \
            -q --tb=line
      - name: Install python-prod optional witness system deps
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'pr-admission' && needs.docs_only_filter.outputs.python_prod_dependency_witness == 'true'
        # Bounded because this step hangs. Observed 2026-08-12 on run 31554774553: stalled
        # >20 min inside `apt-get update` while a neighbouring run completed the identical
        # step in 36 s, consuming the whole 35-minute job clock instead of failing fast and
        # being retryable.
        #
        # Its exposure also rises with the --no-renames fix in this same change: a moved
        # pyproject.toml or ci.yml never reached the witness case before, because the rename
        # collapsed to its destination. It fires correctly now, so a flaky mirror must not be
        # the slowest possible way to find out.
        timeout-minutes: 5
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcairo2-dev libgirepository-2.0-dev gobject-introspection \
            gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0 \
            gir1.2-pango-1.0 \
            ffmpeg \
            portaudio19-dev
      - name: Run python-prod optional dependency witness
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'pr-admission' && needs.docs_only_filter.outputs.python_prod_dependency_witness == 'true'
        # Null-route networked clients so import smokes cannot reach live services from CI.
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          uv run --frozen \
            --extra ci --extra audio --extra tui --extra studio --extra rerank \
            pytest tests/test_python_prod_dependency_constraints.py -q -rs
      - name: Install system deps (cairo, gobject, gst, pango, hook transports)
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcairo2-dev libgirepository-2.0-dev gobject-introspection \
            gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0 \
            gir1.2-pango-1.0 \
            ffmpeg \
            portaudio19-dev \
            socat
      - name: Install Px437 IBM VGA 8x16 font (HARDM aesthetic dependency, audit 4.7 PR #1109)
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        run: |
          # Install system-wide so fontconfig's /usr/share/fonts scan picks it up.
          sudo mkdir -p /usr/share/fonts/TTF
          sudo cp assets/aesthetic-library/fonts/Px437_IBM_VGA_8x16.ttf /usr/share/fonts/TTF/
          # Root cache (scans /usr/share/fonts, writes /var/cache/fontconfig).
          sudo fc-cache -fv /usr/share/fonts
          # User cache. Pango enumerates through the client's fontconfig
          # context; without a user cache, Pango can silently omit freshly
          # installed system fonts (PR #1109 run-24641458881 observed).
          fc-cache -fv
          # fc-list probe confirms fontconfig sees the font.
          fc-list | grep -i 'Px437.*IBM VGA 8x16' \
            || (echo "font install failed (fc-list)" >&2; exit 1)
      - name: Sync uv ci extras for full pytest
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        run: uv sync --extra ci --frozen
      - name: Run python-prod optional dependency witness
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full' && needs.docs_only_filter.outputs.python_prod_dependency_witness == 'true'
        # Null-route networked clients so import smokes cannot reach live services from CI.
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          uv run --frozen \
            --extra ci --extra audio --extra tui --extra studio --extra rerank \
            pytest tests/test_python_prod_dependency_constraints.py -q -rs
      - name: Install Playwright Chromium
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        run: uv run --no-sync playwright install chromium
      - name: Verify Pango can resolve Px437 IBM VGA 8x16
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        # Pango consults fontconfig but has its own per-process cache; a
        # Pango-level probe confirms the HARDM render path will see the
        # font, not just fontconfig. If this fails, HARDM tests render
        # blank cells silently, so fail loud here with diagnostic output.
        run: |
          uv run --no-sync python scripts/ci_verify_pango_font.py "Px437 IBM VGA 8x16"
      - name: Run serial title-card tests
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          uv run --no-sync pytest \
            tests/test_demo_title_cards.py \
            tests/test_demo_video_integration.py \
            -q --tb=line --benchmark-disable
      - name: Run tests
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          # pytest hangs post-session due to open sockets from module-level
          # LiteLLM/Qdrant clients. Root conftest.py has a pytest_sessionfinish
          # that force-closes cached Qdrant + LiteLLM + httpx clients, so
          # teardown exits cleanly (~0.1s) instead of hanging to the timeout.
          #
          # Parallelism: pytest-xdist -n 4 --dist loadfile runs four worker
          # processes, distributed per-file so tests within a file stay
          # serial (preserves file-scoped fixture state + /dev/shm and
          # /tmp write patterns). GitHub Actions ubuntu-latest has 4 vCPUs;
          # -n 4 fully saturates the runner. Bumped from -n 2 in CI/CD
          # speedup PR1 (research drop docs/research/2026-04-26-cicd-speedup.md
          # §2 #2). Monitor first 3 PRs for OOM or socket-conflict regressions;
          # if a flake surfaces, fall back to -n 3.
          #
          # Timeout reduced 25m → 20m after socket-hang fix + xdist. Recent
          # compositor/runtime PRs exercise enough slow end-band tests that
          # the prior 15m wrapper killed otherwise-successful runs at 99%.
          set +e
          timeout -s KILL 1200 \
            uv run --no-sync pytest tests/ -q --tb=line --durations=25 \
              --benchmark-disable \
              --cov=agents --cov=logos --cov=shared \
              --cov-report=xml:coverage.xml --cov-report=term \
              -n 4 --dist loadfile \
              --ignore=tests/hapax_daimonion --ignore=tests/contract \
              --ignore=tests/test_frame_gate.py \
              --ignore=tests/test_hapax_daimonion_pipecat_tts.py \
              --ignore=tests/test_hapax_daimonion_pipeline.py \
              --ignore=tests/test_perception_integration.py \
              --ignore=tests/test_sensor_tier2_tier3.py \
              --ignore=tests/fortress \
              --ignore=tests/test_demo_screenshots.py \
              --ignore=tests/test_demo_screencasts.py \
              --ignore=tests/test_demo_title_cards.py \
              --ignore=tests/test_demo_video_integration.py \
              --ignore=tests/test_demo_video.py \
              --ignore=tests/test_demo_timeline.py \
              --ignore=tests/test_event_log_trace_id.py \
              --ignore=tests/studio_compositor/test_visual_regression_homage.py \
              --ignore=tests/studio_compositor/test_follow_mode.py \
              --ignore=tests/studio_compositor/test_face_obscure_pipeline.py \
              --ignore=tests/test_camera_pipeline_phase2.py \
              --ignore=tests/studio_compositor/test_captions_in_default_layout.py \
              --deselect=tests/studio_compositor/test_assignment_nondestructive.py::test_default_layout_informational_wards_are_non_destructive \
              --deselect=tests/studio_compositor/test_compositor_wiring.py::TestStartLayoutOnly::test_reads_disk_layout_and_populates_state_and_registry \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_exists_and_is_valid_layout \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_source_backends_match_registry_dispatch \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_default_json_chat_ambient_binds_to_chat_ambient_ward \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_load_layout_or_fallback_reads_valid_file \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_fallback_layout_parses_to_same_shape_as_default_json \
              --deselect=tests/studio_compositor/test_default_layout_loading.py::test_load_layout_or_fallback_rescales_to_canvas_size \
              --deselect=tests/studio_compositor/test_overlay_zones_research_gate.py::TestZoneGate::test_default_always_on \
              -m "not llm" -k "not golden and not goldens" \
              -p no:cacheprovider 2>&1 | tee /tmp/pytest-output.txt
          PYTEST_EXIT=${PIPESTATUS[0]}
          # pytest exit codes: 0 = all passed, 1 = failures, 2 = usage error,
          # 3 = internal error, 4 = usage error, 5 = no tests collected.
          # With conftest.py pytest_sessionfinish (#1318) closing socket hangs,
          # the timeout wrapper no longer fires on success, so pytest's own exit
          # code is reliable. The prior grep-for-"failed" parse had a bug:
          # "xfailed" contains "failed" as a substring, so any PR with xfail
          # markers (e.g. #1322 chronic-xfails) flagged a false failure.
          # Trust the exit code directly.
          if [ "$PYTEST_EXIT" -eq 0 ]; then
            # Sanity: a success must have a pytest summary line.
            if grep -qE '\bpassed\b' /tmp/pytest-output.txt; then
              echo "All tests passed (pytest exit 0)"
              exit 0
            fi
            echo "No test summary found — likely crashed or timed out"
            exit 1
          fi
          if [ "$PYTEST_EXIT" -eq 124 ] || [ "$PYTEST_EXIT" -eq 137 ]; then
            # SIGTERM (124) or SIGKILL (137) from the ``timeout`` wrapper.
            echo "Test step timed out (exit $PYTEST_EXIT)"
            exit 1
          fi
          echo "Tests failed (pytest exit $PYTEST_EXIT)"
          exit 1
      - name: Upload Python coverage artifact
        if: always() && needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: python-coverage-xml
          path: coverage.xml
          if-no-files-found: error
          retention-days: 7
      - name: Governance coverage gate
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.test_mode.outputs.mode == 'full'
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: uv run --no-sync python scripts/ci_governance_coverage_gate.py

  codecov-upload:
    needs: [docs_only_filter, post_merge_duplicate_filter, test]
    if: always() && github.event_name == 'push' && needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && needs.test.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Download Python coverage artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-coverage-xml
      - name: Upload Python coverage to Codecov
        continue-on-error: true
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f
        with:
          files: ./coverage.xml
          flags: python
          name: hapax-council-python
          fail_ci_if_error: false
          use_oidc: true

  homage-visual-regression:
    needs: post_merge_duplicate_filter
    if: github.event_name != 'push' || needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Visual-regression goldens are generated on the operator's workstation
    # with locally-installed Px437 IBM VGA + fontconfig cache. CI's Ubuntu
    # runner renders text with different hinting/AA/fallback behaviour, so
    # the pixel-exact goldens drift ~10-20% per ward. Keep the job as a
    # developer-local gate (and for CI visibility via the diff artifacts)
    # but do not block PR merges on it until we ship CI-environment
    # goldens in a follow-on. The pytest step itself runs with
    # continue-on-error so the check surfaces "success" in the PR status
    # list while diff artifacts remain available for local review.
    #
    # PR3 of CI/CD speedup cascade (docs/research/2026-04-26-cicd-speedup.md
    # §2 #5 + §6 + 2026-04-26-cicd-speedup-pr3-5-priorities.md):
    # PATH-GATED. The heavy work (apt deps + uv sync + Px437 font install +
    # pytest) only runs when the PR (or push) actually touches files that
    # could change visual-regression output:
    #   - tests/studio_compositor/test_visual_regression_homage.py
    #   - assets/aesthetic-library/**
    # On all other PRs, the job spins up, evaluates the path filter, and
    # exits in ~5s — the GitHub status check still reports "success" so
    # branch-protection rules aren't disrupted, but the parallel-job slot
    # frees up immediately and ~80s of duplicated apt+uv+font setup is
    # avoided across ~95% of PRs.
    #
    # Cadence concession: skipping homage-vr on most PRs means goldens
    # drift caused by indirect changes (new ward → new rendering path
    # touched by an unrelated module) won't surface until either (a) the
    # operator runs the suite locally during ward iteration, or (b) the
    # nightly cron in `.github/workflows/homage-vr-nightly.yml` runs it
    # on main. The nightly cron preserves the regression cadence at
    # ~24h granularity, well inside the operator's ward-iteration cycle.
    continue-on-error: true
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      # Path filter: detect whether this PR/push touches files that
      # plausibly affect visual-regression output. Outputs `homage_inputs`
      # (string "true"|"false") consumed by every downstream step's `if:`
      # clause. `dorny/paths-filter@v4` handles both pull_request (diff
      # vs base) and push (diff vs before-SHA) automatically.
      - name: Detect homage-vr input changes
        id: filter
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d
        with:
          filters: |
            homage_inputs:
              - 'tests/studio_compositor/test_visual_regression_homage.py'
              - 'assets/aesthetic-library/**'
      - name: Install system deps (cairo, gobject, fonts)
        if: steps.filter.outputs.homage_inputs == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcairo2-dev libgirepository-2.0-dev gobject-introspection \
            gir1.2-pango-1.0 fonts-dejavu-core
      - name: Install Px437 IBM VGA 8x16 font (HARDM aesthetic dependency)
        if: steps.filter.outputs.homage_inputs == 'true'
        run: |
          sudo mkdir -p /usr/share/fonts/TTF
          sudo cp assets/aesthetic-library/fonts/Px437_IBM_VGA_8x16.ttf /usr/share/fonts/TTF/
          sudo fc-cache -fv /usr/share/fonts
          fc-cache -fv
          fc-list | grep -i 'Px437.*IBM VGA 8x16' \
            || (echo "font install failed (fc-list)" >&2; exit 1)
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        if: steps.filter.outputs.homage_inputs == 'true'
        with:
          enable-cache: true
          restore-cache: true
          save-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Sync uv ci extras
        if: steps.filter.outputs.homage_inputs == 'true'
        run: uv sync --extra ci
      - name: Run HOMAGE visual-regression suite (informational; goldens are dev-env pinned)
        id: vr
        if: steps.filter.outputs.homage_inputs == 'true'
        continue-on-error: true
        env:
          LITELLM_BASE_URL: "http://0.0.0.0:1"
          QDRANT_URL: "http://0.0.0.0:1"
          OLLAMA_HOST: "http://0.0.0.0:1"
          OTEL_SDK_DISABLED: "true"
          LANGFUSE_HOST: "http://0.0.0.0:1"
        run: |
          uv run pytest tests/studio_compositor/test_visual_regression_homage.py \
            -q --tb=short
      - name: Upload diff PNGs on failure
        if: steps.filter.outputs.homage_inputs == 'true' && steps.vr.outcome == 'failure'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: homage-visual-regression-diffs
          path: /tmp/homage-visual-regression-diffs/
          if-no-files-found: ignore
          retention-days: 14
      - name: Report visual-regression outcome
        env:
          # GHA-internal step outputs only (boolean-shaped strings); not
          # user-controlled. Bound to env vars per workflow-injection
          # hardening guidelines anyway, so future edits don't silently
          # regress.
          HOMAGE_INPUTS: ${{ steps.filter.outputs.homage_inputs }}
          VR_OUTCOME: ${{ steps.vr.outcome }}
        run: |
          if [ "$HOMAGE_INPUTS" != "true" ]; then
            echo "Skipped — this change does not touch homage-vr inputs."
            echo "  - tests/studio_compositor/test_visual_regression_homage.py"
            echo "  - assets/aesthetic-library/**"
            echo "Nightly cron on main covers golden drift; operator can run locally."
            exit 0
          fi
          echo "visual-regression outcome: $VR_OUTCOME"
          echo "Informational — dev-env goldens diverge from CI fonts/AA/hinting."
          echo "Diff artifacts (if any) are attached to this run for review."

  web-build:
    needs: [docs_only_filter, post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hapax-logos
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        working-directory: .
        run: echo "Merge queue already validated this SHA; web-build reports success."
      - name: Docs-only required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only == 'true'
        working-directory: .
        run: echo "Docs-only change set; web-build reports success without running pnpm."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
      - name: Detect web-build input changes
        id: filter
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d
        with:
          filters: |
            web_build:
              - '.github/workflows/ci.yml'
              - 'hapax-logos/**'
      - name: Non-web required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.web_build != 'true'
        working-directory: .
        run: |
          echo "No web-build inputs changed; web-build reports success without running pnpm."
          echo "Inputs:"
          echo "  - .github/workflows/ci.yml"
          echo "  - hapax-logos/**"
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.web_build == 'true'
        with:
          version: 11
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.web_build == 'true'
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: hapax-logos/pnpm-lock.yaml
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.web_build == 'true'
        run: pnpm install --frozen-lockfile
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.web_build == 'true'
        run: pnpm lint
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.web_build == 'true'
        run: pnpm build

  rust-check:
    needs: post_merge_duplicate_filter
    if: github.event_name != 'push' || needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      # Match hooks/scripts/cargo-check-rust.sh: only crate source edits under
      # hapax-logos/crates/** need the fast Rust check. The job still reports
      # success when skipped so branch protection sees an honest status.
      - name: Detect Rust crate changes
        id: filter
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d
        with:
          filters: |
            rust_crates:
              - 'hapax-logos/Cargo.toml'
              - 'hapax-logos/crates/**'
      - name: Install Rust toolchain
        if: steps.filter.outputs.rust_crates == 'true'
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8
      - name: Install Rust crate system deps
        if: steps.filter.outputs.rust_crates == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm
      - name: Cache cargo registry and build output
        if: steps.filter.outputs.rust_crates == 'true'
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            hapax-logos/target
          key: cargo-crates-${{ runner.os }}-${{ hashFiles('hapax-logos/Cargo.toml', 'hapax-logos/crates/**/Cargo.toml') }}
          restore-keys: |
            cargo-crates-${{ runner.os }}-
      - name: Run crate cargo checks
        if: steps.filter.outputs.rust_crates == 'true'
        working-directory: hapax-logos
        run: |
          set -euo pipefail
          found=0
          for manifest in crates/*/Cargo.toml; do
            [ -f "$manifest" ] || continue
            crate="$(basename "$(dirname "$manifest")")"
            cargo check -p "$crate"
            found=1
          done
          if [ "$found" -eq 0 ]; then
            echo "No hapax-logos/crates/* Cargo.toml files found."
          fi
      - name: Report skipped Rust check
        if: steps.filter.outputs.rust_crates != 'true'
        run: echo "Skipped — this change does not touch hapax-logos Rust crate inputs."

  vscode-build:
    needs: [docs_only_filter, post_merge_duplicate_filter]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vscode
    steps:
      - name: Post-merge duplicate required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group == 'true'
        working-directory: .
        run: echo "Merge queue already validated this SHA; vscode-build reports success."
      - name: Docs-only required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only == 'true'
        working-directory: .
        run: echo "Docs-only change set; vscode-build reports success without running pnpm."
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
      - name: Detect vscode-build input changes
        id: filter
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true'
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d
        with:
          filters: |
            vscode_build:
              - '.github/workflows/ci.yml'
              - 'vscode/**'
      - name: Non-vscode required-check sentinel
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.vscode_build != 'true'
        working-directory: .
        run: |
          echo "No vscode-build inputs changed; vscode-build reports success without running pnpm."
          echo "Inputs:"
          echo "  - .github/workflows/ci.yml"
          echo "  - vscode/**"
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.vscode_build == 'true'
        with:
          version: 11
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.vscode_build == 'true'
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: vscode/pnpm-lock.yaml
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.vscode_build == 'true'
        run: pnpm install --frozen-lockfile
      - if: needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true' && needs.docs_only_filter.outputs.docs_only != 'true' && steps.filter.outputs.vscode_build == 'true'
        run: pnpm run build

  secrets-scan:
    needs: post_merge_duplicate_filter
    if: github.event_name != 'push' || needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          # Shallow clone vs full-history. gitleaks scans current diff;
          # 50 commits is sufficient for typical PRs.
          fetch-depth: 50
      - name: Run Gitleaks CLI
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_SHA: ${{ github.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          log_opts="--max-count=50"
          if [ "$GITHUB_EVENT_NAME" = "merge_group" ]; then
            # Merge queue uses a synthetic squash commit; scanning 50 linear
            # ancestors there walks unrelated parent history.
            log_opts="--max-count=1"
          elif [ "$GITHUB_EVENT_NAME" = "push" ]; then
            if [ -n "$PUSH_BEFORE_SHA" ] && [ "$PUSH_BEFORE_SHA" != "0000000000000000000000000000000000000000" ]; then
              git fetch --quiet origin "$PUSH_BEFORE_SHA" || true
              if git cat-file -e "$PUSH_BEFORE_SHA^{commit}" 2>/dev/null; then
                log_opts="$PUSH_BEFORE_SHA..$GITHUB_SHA"
              else
                echo "Push before SHA is unavailable in checkout; scanning current commit only."
                log_opts="--max-count=1"
              fi
            else
              log_opts="--max-count=1"
            fi
          fi
          docker run --rm \
            --entrypoint sh \
            -e GITLEAKS_LOG_OPTS="$log_opts" \
            -v "$PWD":/repo \
            -w /repo \
            ghcr.io/gitleaks/gitleaks:v8.30.1 \
            -c 'git config --global --add safe.directory /repo && gitleaks git --redact --log-opts="$GITLEAKS_LOG_OPTS" /repo'

  security:
    needs: post_merge_duplicate_filter
    if: github.event_name != 'push' || needs.post_merge_duplicate_filter.outputs.duplicate_merge_group != 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install bandit
      - name: Run Bandit security scan
        run: bandit -r . -x ./tests,./hapax-logos,./vscode --severity-level medium -f json -o bandit-report.json || true
      - name: Check for high-severity findings
        run: |
          if command -v jq &>/dev/null; then
            HIGH=$(jq '[.results[] | select(.issue_severity == "HIGH")] | length' bandit-report.json 2>/dev/null || echo 0)
            if [ "$HIGH" -gt 0 ]; then
              echo "::error::Bandit found $HIGH high-severity security issues"
              jq '.results[] | select(.issue_severity == "HIGH") | "\(.filename):\(.line_number) \(.issue_text)"' bandit-report.json
              exit 1
            fi
          fi
          echo "No high-severity security issues found"

  all-green:
    # Single aggregate required context (task reform-native-merge-queue). Collapses
    # the literal required checks into one job so the merge queue can't be
    # permanently stalled by a required-context-name mismatch. `if: always()` + treating `skipped` as OK
    # preserves the fast/slow split: `test-full-shard` is merge_group-only, so it
    # is `skipped` on pull_request events and all-green still passes there.
    name: all-green
    needs: [lint, capability-surface-delta, egress-boundary-pin, typecheck, test, test-full-shard, web-build, vscode-build, public_surface_freshness]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate required CI results
        env:
          RESULTS: ${{ join(needs.*.result, ' ') }}
        run: |
          set -euo pipefail
          echo "Aggregated required-job results: $RESULTS"
          failed=0
          for r in $RESULTS; do
            case "$r" in
            success | skipped) ;;
            *)
              echo "::error::A required CI job concluded '$r'"
              failed=1
              ;;
            esac
          done
          if [ "$failed" -ne 0 ]; then
            echo "all-green: FAIL — at least one required CI job did not succeed."
            exit 1
          fi
          echo "all-green: PASS — all required CI jobs succeeded or were skipped."
